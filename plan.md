# Plan: Go Live + English Documentation

## Changes

1. **Server:** Listen on `0.0.0.0` (all network interfaces), permissive CORS in dev
2. **Vite:** Dev server binds to `0.0.0.0` so phone can connect
3. **Scripts:** Add `dev:host` (network-accessible dev), `prod` (build + serve) scripts
4. **README.md → English**
5. **PROJECT.md → English**
6. **package.json description → English**
7. Push to GitHub
